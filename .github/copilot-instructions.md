> **Instructions for Copilot agent**: Generate a minimal working scaffold for the foundational infrastructure of the full-stack web application described in `/docs/architecture/phase-1-bootstrap.md`. Follow every constraint and decision listed precisely. Where a choice is marked **[DECISION]**, implement exactly as stated — do not substitute alternatives. Where a section says **[SCAFFOLD]**, generate representative, working boilerplate — not placeholder comments or empty files. The result must be a runnable application: `npm install && npm run dev` must start the stack with no manual configuration steps beyond copying `.env.example` to `.env` If any architectural, implementation, dependency or other important decision is unclear or materially underspecified, stop and ask for clarification before proceeding. Do not invent frameworks, libraries, rendering patterns, or infrastructure not explicitly specified in this document. If multiple valid implementation approaches exist, prefer the simplest approach consistent with the documented architectural goals. Before introducing a dependency, verify it is necessary and consistent with the server-first and progressive-enhancement principles of the scaffold. Do not silently substitute alternatives for any item marked [DECISION]. If a generated implementation conflicts with another section of this specification, stop and ask for clarification rather than guessing priority. In general terms, if in doubt on any decision, stop and ask for clarification.
